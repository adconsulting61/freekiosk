# Port the server listens on
PORT=3000

# Optional: secret token to prevent unauthorized access to the stream
# Set this and add it to the kiosk "relay secret" field in settings
# Leave blank to disable auth (only do this on a private network)
STREAM_SECRET=change-me-to-a-random-string
